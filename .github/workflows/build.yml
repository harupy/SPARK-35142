name: Test

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        pyspark-version: ["2.4.7", "3.0.0", "3.0.2", "3.1.1"]
    steps:
      - uses: actions/setup-python@v2
        with:
          python-version: "3.7"
      - run: |
          pip install scikit-learn pyspark==${{ matrix.pyspark-version }}
      - run: |
          python one_vs_rest.py
